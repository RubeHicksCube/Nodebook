# Postgres DB credentials (used by docker compose)
POSTGRES_USER=nodebook
POSTGRES_PASSWORD=nodebook
POSTGRES_DB=nodebook
DB_PORT=5432

# JWT secret for backend
JWT_SECRET=super-secret-change-this

# Optional: override frontend/backend ports
# VITE_API_URL=http://localhost:4000/api
